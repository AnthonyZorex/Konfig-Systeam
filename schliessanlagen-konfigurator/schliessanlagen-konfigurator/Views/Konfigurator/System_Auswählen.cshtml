@model schliessanlagen_konfigurator.Models.Orders;
@using Microsoft.AspNetCore.Http;
﻿@using System.Text.RegularExpressions;
@{
    ViewData["Title"] = $"System_Auswählen/{Model.userKey}";
}

    <div>
        <h3>
            System auswählen
        </h3>

        <p>
            Lorem ipsum dolor sit amet, consectetuer adipiscing elit. Aenean commodo ligula eget dolor. Aenean massa. Cum sociis natoque penatibus et magnis dis parturient montes, nascetur
            ridiculus mus. Donec quam felis, ultricies nec, pellentesque eu, pretium quis, sem. Nulla consequat massa quis enim. Donec pede justo, fringilla vel, aliquet nec, vulputate eget, arcu. In enim
            justo, rhoncus ut, imperdiet a, venenatis vitae, justo. Nullam dictum felis eu pede mollis pretium. Integer tincidunt.
        </p>
    </div>
     

        <div id="ExelMenu">
    <form asp-action="System_Auswählen" method="post" enctype="multipart/form-data" asp-controller="Konfigurator">
        @Html.HiddenFor(m => m.userKey)
       
        @if (ViewBag.Doppel != null && ViewBag.Knaufzylinder == null && ViewBag.Halb == null && ViewBag.Hebel == null && ViewBag.VorhanSchloss == null && ViewBag.Aussen == null)
        {
            foreach (var item in ViewBag.Doppel)
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                           <a class="btn btn-danger" id="chekProduct" name="DopelId" href=" @Url.Action("OrdersKey", "Konfigurator", new { DopelId = item.Id , param2 = item.userKey})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        }
        @if (ViewBag.Knaufzylinder != null && ViewBag.Doppel == null && ViewBag.Halb == null && ViewBag.Hebel == null && ViewBag.VorhanSchloss == null && ViewBag.Aussen == null)
        {
            foreach (var item in ViewBag.Knaufzylinder)
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                            <a class="btn btn-danger" id="chekProduct" name="KnayfID" href=" @Url.Action("OrdersKey", "Konfigurator", new { KnayfID = item.Id , param2 = item.userKey})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        }
        @if (ViewBag.Halb != null && ViewBag.Doppel == null && ViewBag.Knaufzylinder == null && ViewBag.Hebel == null && ViewBag.VorhanSchloss == null && ViewBag.Aussen == null)
        {
            foreach (var item in ViewBag.Halb )
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                            <a class="btn btn-danger" id="chekProduct" name="Halb" href=" @Url.Action("OrdersKey", "Konfigurator", new { Halb = item.Id , param2 = item.userKey})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        }
        @if (ViewBag.Hebel != null && ViewBag.Doppel == null && ViewBag.Knaufzylinder == null && ViewBag.Halb == null && ViewBag.VorhanSchloss == null && ViewBag.Aussen == null)
        {
            foreach (var item in ViewBag.Hebel)
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                            <a class="btn btn-danger" id="chekProduct" name="Hebel" href=" @Url.Action("OrdersKey", "Konfigurator", new { Hebel = item.Id , param2 = item.userKey})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        }
        @if (ViewBag.VorhanSchloss != null && ViewBag.Doppel == null && ViewBag.Knaufzylinder == null && ViewBag.Halb == null && ViewBag.Hebel == null && ViewBag.Aussen == null)
        {
            foreach (var item in ViewBag.VorhanSchloss)
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                            <a class="btn btn-danger" id="chekProduct" name="Vorhan" href=" @Url.Action("OrdersKey", "Konfigurator", new { Vorhan = item.Id , param2 = item.userKey})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        }
        @if (ViewBag.Aussen != null && ViewBag.Doppel == null && ViewBag.Knaufzylinder == null && ViewBag.Halb == null && ViewBag.Hebel == null && ViewBag.VorhanSchloss == null)
        {
            foreach (var item in ViewBag.Aussen)
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                            <a class="btn btn-danger" id="chekProduct" name="Aussen" href=" @Url.Action("OrdersKey", "Konfigurator", new { Aussen = item.Id , param2 = item.userKey})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        }
    @*     @if (ViewBag.Doppel != null && ViewBag.Halb != null)
        {
            foreach (var item in ViewBag.Doppel)
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                            <a class="btn btn-danger" id="chekProduct" name="DopelId" href=" @Url.Action("OrdersKey", "Konfigurator", new { DopelId = item.Id , param2 = item.userKey,Halb = item?.cheked3})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        }*@
        @if (ViewBag.Doppel != null && ViewBag.Knaufzylinder != null && ViewBag.Halb == null && ViewBag.Hebel == null && ViewBag.VorhanSchloss == null && ViewBag.Aussen == null)
        {
            foreach (var item in ViewBag.Doppel)
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                            <a class="btn btn-danger" id="chekProduct" name="DopelId" href=" @Url.Action("OrdersKey", "Konfigurator", new { DopelId = item.Id , param2 = item.userKey,KnayfID = item?.cheked2})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        } 
        @if (ViewBag.Doppel != null && ViewBag.Knaufzylinder != null && ViewBag.Halb != null && ViewBag.Hebel != null && ViewBag.VorhanSchloss != null && ViewBag.Aussen != null)
        {
            var s = @ViewBag.Doppel.Count;
            foreach (var item in ViewBag.Doppel)
            {

                <div id="ExelItem">
                    <div id="productImage">
                        <img decoding="async" style="border: 2px solid rgba(237,237,237,0.5);" src="@("~/image/"+@item.ImageName)" asp-append-version="true" width="300px" height="300px" />
                    </div>
                    <div id="productMenuInfo">
                        <div id="ProductNameUndCost">

                            <div>
                                <h3>@item.Name</h3>
                            </div>

                            <div style="margin-left: auto;">
                                <h4 id="cost">ab @item.Cost €</h4>
                            </div>

                        </div>

                        <div id="product_desciptions">
                            <h5>@item.description</h5>
                        </div>
                        <div id="productChek">

                            <a class="btn btn-danger" id="chekProduct" name="DopelId" href=" @Url.Action("OrdersKey", "Konfigurator", new { DopelId = item?.Id , param2 = item.userKey,KnayfID = item?.cheked2, Halb = item?.cheked3,Hebel = item?.cheked4,Vorhan = item?.cheked5,Aussen= item?.cheked6})">WÄHLEN</a>

                        </div>
                    </div>


                </div>
            }
        }
    </form>

        </div>

 <script>
  
</script>
